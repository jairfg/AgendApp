
    <div class="card">
        <div class="card-header">
            <h3> {{client.name}}</h3>
             <p> Registro :  {{tiempo client.createdAt}} </p>
             <p> Ultima actualización : {{tiempo client.updatedAt}}</p>
        </div>
        <div class="card-body">
            <form action="/client/edit/{{client.id}}?_method=PUT" method="POST">
            <div class="row">
                <div class="col">
                <div class="form-group">
                    <input type="text" name="name"  placeholder ="Nombre Completo" class="form-control" value="{{client.name}}">
                </div>
                   <div class="form-group">
                    <input type="text" name="nro_identidad"  placeholder ="DNI"class="form-control" value="{{client.nro_identidad}}">
                </div>
                 <div class="form-group">
                    <input type="email" name="email" class="form-control" value="{{client.email}}">
                </div>
                <div class="form-group">
                    <input type="phone" name="phone" class="form-control" value="{{client.phone}}">
                </div>
                 <div class="form-group">
                    <textarea name="description" class="form-control" cols="30" rows="10">{{client.description}}</textarea>
                </div>  
                </div>
            <div class="col">
                <div class="form-group">
                        
                        </div>
                    </div>
                </div>
                <div class="form-group text-center">
                                    <button class="btn btn-primary " type="submit">
                                        Guardar
                                    </button>
                                </div>
            </form>
        </div>
    </div>

